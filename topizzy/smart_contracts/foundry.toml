[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test/audit"   # broken test/Airtime.t.sol preserved in test/ as evidence for finding I-1

# Audit profile — explicit alias for running PoC tests
[profile.audit]
src = "src"
out = "out"
libs = ["lib"]
test = "test/audit"

# See more config options https://github.com/foundry-rs/foundry/blob/master/crates/config/README.md#all-options
